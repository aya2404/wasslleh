# دليل تركيب وإعداد مفاتيح API
## خطوة بخطوة - موقع وصلة

---

## 🚀 البدء السريع

هذا الدليل التفصيلي يوضح كيفية الحصول على وإعداد جميع مفاتيح API المطلوبة لتشغيل موقع وصلة بكامل ميزاته.

---

## 📋 قائمة المراجعة السريعة

قبل البدء، تأكد من وجود:
- [ ] حساب Google Cloud Platform
- [ ] حساب LinkedIn Developer
- [ ] حساب Amazon Associate
- [ ] حساب Google AI Studio (Gemini)
- [ ] مشروع Supabase
- [ ] محرر نصوص للتعديل على الملفات

---

## 1. 🗺️ إعداد Google Maps API

### الخطوة 1: إنشاء مشروع Google Cloud
1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. انقر على "إنشاء مشروع جديد"
3. أدخل اسم المشروع: "Waslah Platform"
4. حدد منظمتك (اختياري)
5. انقر "إنشاء"

### الخطوة 2: تفعيل APIs المطلوبة
```bash
# من Cloud Shell أو الطرفية المحلية مع gcloud CLI
gcloud services enable maps-backend.googleapis.com
gcloud services enable places-backend.googleapis.com
gcloud services enable directions-backend.googleapis.com
gcloud services enable geocoding-backend.googleapis.com
```

أو من الواجهة:
1. اذهب إلى "APIs & Services" > "Library"
2. ابحث عن وفعل:
   - Maps JavaScript API
   - Places API
   - Directions API
   - Geocoding API

### الخطوة 3: إنشاء مفتاح API
1. اذهب إلى "APIs & Services" > "Credentials"
2. انقر "Create Credentials" > "API Key"
3. انسخ المفتاح المُنشأ
4. انقر "Restrict Key" لتحديد الصلاحيات

### الخطوة 4: تحديد القيود الأمنية
```json
{
  "allowedReferers": [
    "https://waslah.com/*",
    "https://*.waslah.com/*",
    "http://localhost:3000/*"
  ],
  "apis": [
    "Maps JavaScript API",
    "Places API",
    "Directions API",
    "Geocoding API"
  ]
}
```

### إضافة المفتاح لملف البيئة:
```env
GOOGLE_MAPS_API_KEY_DEV=AIzaSyABC123...  # استبدل بمfتاحك الفعلي
GOOGLE_MAPS_API_KEY_PROD=AIzaSyDEF456... # مفتاح منفصل للإنتاج
```

---

## 2. 💼 إعداد LinkedIn API

### الخطوة 1: إنشاء تطبيق LinkedIn
1. اذهب إلى [LinkedIn Developer Portal](https://developer.linkedin.com/)
2. انقر "Create App"
3. املأ البيانات:
   - **App name**: Waslah Platform
   - **LinkedIn Page**: صفحة شركتك
   - **Privacy policy URL**: https://waslah.com/privacy
   - **App logo**: شعار وصلة

### الخطوة 2: طلب الصلاحيات
1. اذهب إلى تبويب "Products"
2. اطلب:
   - Sign In with LinkedIn
   - Share on LinkedIn
   - Marketing Developer Platform (للوظائف)

### الخطوة 3: إعداد OAuth 2.0
```javascript
// إعدادات OAuth
const LINKEDIN_CONFIG = {
  client_id: "YOUR_CLIENT_ID",
  redirect_uri: "https://waslah.com/auth/linkedin/callback",
  scope: "r_liteprofile r_emailaddress w_member_social"
};
```

### الخطوة 4: الحصول على المفاتيح
1. اذهب إلى تبويب "Auth"
2. انسخ:
   - Client ID
   - Client Secret

### إضافة المفاتيح لملف البيئة:
```env
LINKEDIN_CLIENT_ID=86abc123def456
LINKEDIN_CLIENT_SECRET=XYZ789uvwxyz123
LINKEDIN_REDIRECT_URI=https://waslah.com/auth/linkedin/callback
```

---

## 3. 🛒 إعداد Amazon API

### الخطوة 1: الانضمام لبرنامج Amazon Associates
1. اذهب إلى [Amazon Associates](https://affiliate-program.amazon.com/)
2. انقر "Join Now for Free"
3. املأ معلومات الحساب
4. أدخل معلومات الموقع: https://waslah.com
5. اختر طرق الدفع

### الخطوة 2: الحصول على Associate Tag
```
بعد الموافقة، ستحصل على:
Associate Tag: waslah-20  (مثال)
```

### الخطوة 3: إعداد Product Advertising API
1. اذهب إلى [Amazon Developer Console](https://developer.amazon.com/)
2. سجل دخول بحساب Associates
3. اذهب إلى "Product Advertising API"
4. اطلب الوصول للـ API

### الخطوة 4: إنشاء مفاتيح الوصول
```bash
# في AWS Console
1. اذهب إلى IAM
2. إنشاء مستخدم جديد
3. إرفاق سياسة ProductAdvertisingAPI
4. إنشاء Access Key
```

### إضافة المفاتيح لملف البيئة:
```env
AMAZON_ACCESS_KEY=AKIAIOSFODNN7EXAMPLE
AMAZON_SECRET_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AMAZON_ASSOCIATE_TAG=waslah-20
AMAZON_REGION=us-east-1
```

---

## 4. 🤖 إعداد Gemini AI API

### الخطوة 1: الوصول لـ Google AI Studio
1. اذهب إلى [Google AI Studio](https://makersuite.google.com/)
2. سجل دخول بحساب Google
3. وافق على شروط الاستخدام

### الخطوة 2: إنشاء مفتاح API
1. انقر "Get API Key"
2. اختر مشروع Google Cloud (أو أنشئ جديد)
3. انقر "Create API Key"
4. انسخ المفتاح

### الخطوة 3: اختبار المفتاح
```bash
curl -H 'Content-Type: application/json' \
     -d '{"contents":[{"parts":[{"text":"Hello, Gemini!"}]}]}' \
     -X POST "https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent?key=YOUR_KEY"
```

### إضافة المفتاح لملف البيئة:
```env
GEMINI_API_KEY=AIzaSyABC123def456GHI789jkl
GEMINI_MODEL=gemini-pro
GEMINI_ENDPOINT=https://generativelanguage.googleapis.com
```

---

## 5. 🔊 إعداد Text-to-Speech API

### الخيار الأول: Google Cloud TTS
1. في نفس مشروع Google Cloud
2. فعل Cloud Text-to-Speech API
3. استخدم نفس مفتاح API أو أنشئ مفتاح مخصص

```env
GOOGLE_TTS_API_KEY=AIzaSyABC123...
GOOGLE_PROJECT_ID=waslah-platform-12345
```

### الخيار الثاني: Azure Cognitive Services (بديل)
1. اذهب إلى [Azure Portal](https://portal.azure.com/)
2. إنشاء "Cognitive Services" resource
3. اختر "Speech Services"
4. انسخ الـ Key والـ Region

```env
AZURE_TTS_SUBSCRIPTION_KEY=abc123def456...
AZURE_TTS_REGION=eastus
```

---

## 6. 🗄️ إعداد Supabase

### الخطوة 1: إنشاء مشروع Supabase
1. اذهب إلى [Supabase](https://supabase.com/)
2. انقر "New Project"
3. اختر المنظمة أو أنشئ جديدة
4. املأ بيانات المشروع:
   - **Name**: Waslah Platform
   - **Database Password**: كلمة مرور قوية
   - **Region**: اختر أقرب منطقة

### الخطوة 2: الحصول على المفاتيح
1. اذهب إلى Settings > API
2. انسخ:
   - Project URL
   - anon public key
   - service_role secret key

### الخطوة 3: إعداد قاعدة البيانات
```sql
-- إنشاء جدول المستخدمين
CREATE TABLE user_profiles (
    id UUID REFERENCES auth.users(id) PRIMARY KEY,
    name TEXT,
    disability_type TEXT,
    preferences JSONB,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### إضافة المفاتيح لملف البيئة:
```env
SUPABASE_URL=https://xyzcompany.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

## 🔧 إعداد ملفات التكوين

### 1. إنشاء ملف .env
```bash
# في جذر المشروع
touch .env
touch .env.local
touch .env.production
```

### 2. ملء ملف .env
```env
# ================================================
# متغيرات البيئة - موقع وصلة
# تأكد من عدم رفع هذا الملف للـ repository العام
# ================================================

# بيئة التطوير
NODE_ENV=development
APP_ENV=development

# Google Maps API
GOOGLE_MAPS_API_KEY_DEV=AIzaSyABC123...
GOOGLE_MAPS_API_KEY_PROD=AIzaSyDEF456...
GOOGLE_MAPS_API_KEY_STAGING=AIzaSyGHI789...

# LinkedIn API
LINKEDIN_CLIENT_ID=86abc123def456
LINKEDIN_CLIENT_SECRET=XYZ789uvwxyz123
LINKEDIN_REDIRECT_URI=http://localhost:3000/auth/linkedin/callback

# Amazon API
AMAZON_ACCESS_KEY=AKIAIOSFODNN7EXAMPLE
AMAZON_SECRET_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AMAZON_ASSOCIATE_TAG=waslah-20
AMAZON_REGION=us-east-1

# Gemini AI API
GEMINI_API_KEY=AIzaSyABC123def456GHI789jkl
GEMINI_MODEL=gemini-pro
GEMINI_ENDPOINT=https://generativelanguage.googleapis.com

# Text-to-Speech APIs
GOOGLE_TTS_API_KEY=AIzaSyABC123...
GOOGLE_PROJECT_ID=waslah-platform-12345
AZURE_TTS_SUBSCRIPTION_KEY=abc123def456...
AZURE_TTS_REGION=eastus

# Supabase
SUPABASE_URL=https://xyzcompany.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# إعدادات التشفير
ENCRYPTION_SECRET_KEY=your-very-secret-encryption-key-here-32-chars-minimum

# إعدادات إضافية
API_TIMEOUT=30000
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX_REQUESTS=100
```

### 3. إعداد .gitignore
```gitignore
# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# API Keys
config/keys/
*.key
*.pem
secrets/
```

---

## 🔍 اختبار المفاتيح

### سكريبت اختبار شامل
```typescript
// scripts/test-api-keys.ts
import { APIKeysManager } from '../config/apiKeysManager';

async function testAllAPIKeys() {
  console.log('🧪 اختبار مفاتيح API...\n');

  // اختبار Google Maps
  await testGoogleMaps();
  
  // اختبار LinkedIn
  await testLinkedIn();
  
  // اختبار Amazon
  await testAmazon();
  
  // اختبار Gemini
  await testGemini();
  
  // اختبار TTS
  await testTTS();
  
  // اختبار Supabase
  await testSupabase();
  
  console.log('\n✅ انتهى اختبار جميع المفاتيح');
}

async function testGoogleMaps() {
  try {
    const key = process.env.GOOGLE_MAPS_API_KEY_DEV;
    const response = await fetch(`https://maps.googleapis.com/maps/api/geocode/json?address=Amman,Jordan&key=${key}`);
    const data = await response.json();
    
    if (data.status === 'OK') {
      console.log('✅ Google Maps API - يعمل بشكل صحيح');
    } else {
      console.log('❌ Google Maps API - خطأ في المفتاح');
    }
  } catch (error) {
    console.log('❌ Google Maps API - فشل في الاتصال');
  }
}

// تابع باقي الاختبارات...
```

### تشغيل الاختبار
```bash
npx ts-node scripts/test-api-keys.ts
```

---

## 🚀 النشر والإنتاج

### 1. إعداد متغيرات البيئة للإنتاج

#### Vercel
```bash
vercel env add GOOGLE_MAPS_API_KEY_PROD
vercel env add LINKEDIN_CLIENT_ID
vercel env add LINKEDIN_CLIENT_SECRET
# ... باقي المتغيرات
```

#### Netlify
```bash
# في ملف netlify.toml
[build.environment]
  GOOGLE_MAPS_API_KEY_PROD = "your-production-key"
  LINKEDIN_CLIENT_ID = "your-linkedin-id"
  # ... باقي المتغيرات
```

#### Railway / Render
1. اذهب إلى لوحة التحكم
2. أضف Environment Variables
3. أدخل جميع المتغيرات المطلوبة

### 2. فحص الأمان قبل النشر
```bash
# تحقق من عدم وجود مفاتيح في الكود
grep -r "AIzaSy" src/ --exclude-dir=node_modules
grep -r "sk-" src/ --exclude-dir=node_modules
grep -r "AKIA" src/ --exclude-dir=node_modules
```

---

## 🛡️ أفضل الممارسات الأمنية

### 1. تدوير المفاتيح دورياً
```bash
# سكريبت تدوير المفاتيح
#!/bin/bash
echo "🔄 بدء تدوير مفاتيح API..."

# إنشاء مفاتيح جديدة
# تحديث البيئات المختلفة
# اختبار المفاتيح الجديدة
# إزالة المفاتيح القديمة

echo "✅ تم تدوير جميع المفاتيح بنجاح"
```

### 2. مراقبة الاستخدام
```typescript
// utils/apiUsageMonitor.ts
export class APIUsageMonitor {
  static async logUsage(apiName: string, endpoint: string) {
    const usage = {
      api: apiName,
      endpoint,
      timestamp: new Date(),
      rateLimitRemaining: response.headers['x-ratelimit-remaining']
    };
    
    // حفظ في قاعدة البيانات
    await supabase.from('api_usage').insert(usage);
  }
}
```

### 3. إنشاء تنبيهات
```typescript
// utils/apiAlerts.ts
export class APIAlerts {
  static async checkRateLimits() {
    // فحص حدود الاستخدام
    // إرسال تنبيهات عند الاقتراب من الحد
  }
  
  static async notifyOnErrors(error: APIError) {
    // إرسال تنبيه فوري للمطورين
  }
}
```

---

## 🔧 استكشاف الأخطاء وإصلاحها

### مشاكل شائعة وحلولها

#### 1. خطأ "API key not valid"
```
الحل:
✅ تحقق من صحة المفتاح
✅ تأكد من تفعيل الـ API المطلوبة
✅ راجع القيود المطبقة على المفتاح
```

#### 2. خطأ "Quota exceeded"
```
الحل:
✅ راجع حدود الاستخدام الشهرية
✅ فعل الفوترة إذا لزم الأمر
✅ حسّن استخدام الـ API
```

#### 3. خطأ "Access denied"
```
الحل:
✅ تحقق من صلاحيات المفتاح
✅ راجع whitelist الـ domains
✅ تأكد من صحة headers الطلب
```

### سكريبت التشخيص
```bash
#!/bin/bash
# scripts/diagnose-apis.sh

echo "🔍 تشخيص حالة APIs..."

# فحص اتصال الإنترنت
ping -c 1 google.com > /dev/null && echo "✅ الاتصال بالإنترنت" || echo "❌ لا يوجد اتصال"

# فحص متغيرات البيئة
[ -z "$GOOGLE_MAPS_API_KEY_DEV" ] && echo "❌ مفقود: GOOGLE_MAPS_API_KEY_DEV" || echo "✅ موجود: GOOGLE_MAPS_API_KEY_DEV"

# فحص صحة المفاتيح
node scripts/test-api-keys.js
```

---

## 📞 الدعم والمساعدة

### إذا واجهت مشاكل:

1. **راجع هذا الدليل مرة أخرى**
2. **تحقق من ملفات السجل (logs)**
3. **اتصل بالدعم التقني**

### معلومات الاتصال:
- **البريد الإلكتروني**: api-support@waslah.com
- **الواتساب**: +962-X-XXXX-XXXX
- **الدعم المباشر**: متوفر من 9 صباحاً - 6 مساءً

### الموارد المفيدة:
- [Google Cloud Documentation](https://cloud.google.com/docs)
- [LinkedIn API Documentation](https://docs.microsoft.com/en-us/linkedin/)
- [Amazon Product Advertising API](https://webservices.amazon.com/paapi5/documentation/)
- [Gemini AI Documentation](https://ai.google.dev/docs)
- [Supabase Documentation](https://supabase.com/docs)

---

*تم إعداد هذا الدليل بتاريخ: سبتمبر 2024*
*آخر تحديث: سبتمبر 2024*

---

© 2024 وصلة - جسرٌ نحو مجتمع شامل. جميع الحقوق محفوظة.